# 数据配置说明（DATA_CONFIG.md）

## 📝 概述

所有应用配置数据都存储在 `data.json` 文件中，方便修改和调整，无需修改代码。

## 📂 data.json 文件结构

### 1. 康复阶段配置（phases）

配置四个康复阶段的详细信息：

```json
{
  "phases": {
    "protection": {
      "name": "保护期",
      "icon": "🛡️",
      "daysRange": [0, 13],
      "hint": "阶段提示文字",
      "exerciseTarget": 15,
      "exerciseTargets": null
    }
  }
}
```

**字段说明：**
- `name`: 阶段名称
- `icon`: 阶段图标（Emoji）
- `daysRange`: 天数范围 `[起始天, 结束天]`
- `hint`: 阶段提示文字
- `exerciseTarget`: 固定运动目标（分钟）
- `exerciseTargets`: 分段运动目标（如恢复期根据天数不同）
  ```json
  "exerciseTargets": {
    "0-6": 20,
    "7-13": 25,
    "14-27": 30,
    "28+": 35
  }
  ```

**强化模式配置（仅强心期）：**
- `intensifiedHint`: 强化模式提示文字
- `intensifiedExerciseTarget`: 强化模式运动目标

---

### 2. 血压心率正常范围（vitalSigns）

```json
{
  "vitalSigns": {
    "bloodPressure": {
      "systolic": {
        "normal": [90, 140],
        "dangerLow": 90,
        "dangerHigh": 180,
        "warningHigh": 140
      },
      "diastolic": {
        "normal": [60, 90],
        "dangerLow": 60,
        "dangerHigh": 110,
        "warningHigh": 90
      }
    },
    "heartRate": {
      "normal": [60, 100],
      "dangerLow": 50,
      "dangerHigh": 120,
      "warningLow": 60,
      "warningHigh": 100
    }
  }
}
```

**字段说明：**
- `normal`: 正常范围 `[最小值, 最大值]`
- `dangerLow`: 危险低值
- `dangerHigh`: 危险高值
- `warningLow`: 警告低值
- `warningHigh`: 警告高值

---

### 3. 术后检查配置（checkups）

```json
{
  "checkups": {
    "firstLipid": {
      "name": "血脂检查（第一次）",
      "dueStartDays": 28,
      "dueEndDays": 56,
      "hasLDL": true,
      "hasEF": false
    },
    "secondLipid": {
      "name": "血脂检查（第二次）",
      "dueDays": 90,
      "hasLDL": true,
      "hasEF": false
    }
  }
}
```

**字段说明：**
- `name`: 检查项目名称
- `dueStartDays`: 推荐开始时间（术后天数）
- `dueEndDays`: 推荐结束时间（术后天数）
- `dueDays`: 固定时间（术后天数）
- `hasLDL`: 是否包含LDL检测
- `hasEF`: 是否包含EF检测

---

### 4. LDL 和 EF 达标阈值

```json
{
  "ldlThresholds": {
    "good": 1.4,
    "warning": 1.8
  },
  "efThresholds": {
    "good": 50,
    "warning": 40
  }
}
```

**字段说明：**
- **LDL（低密度脂蛋白）：**
  - < `good` (1.4)：达标（绿色）
  - `good` ~ `warning` (1.4~1.8)：需要注意（橙色）
  - ≥ `warning` (1.8)：危险（红色）

- **EF（射血分数）：**
  - ≥ `good` (50)：正常（绿色）
  - `warning` ~ `good` (40~50)：需要注意（橙色）
  - < `warning` (40)：高风险（红色）

---

### 5. 蛋白质食物推荐（proteinRecommendations）

```json
{
  "proteinRecommendations": {
    "protection": [
      {
        "name": "清蒸鲈鱼",
        "amount": "50–80g",
        "meal": "午餐 / 晚餐",
        "note": "极少盐，不放油，易消化"
      }
    ],
    "recovery": [...],
    "strengthening": [...],
    "maintenance": [...]
  }
}
```

**字段说明：**
- `name`: 食物名称
- `amount`: 推荐分量
- `meal`: 建议餐次
- `note`: 营养提示（可为空）

**阶段分类：**
- `protection`: 保护期（0-13天）
- `recovery`: 恢复期（14-55天）
- `strengthening`: 强心期（56-180天）
- `maintenance`: 维护期（180天+）

---

## 🛠️ 如何修改配置

### 修改阶段天数范围

如果想调整恢复期的时间：

```json
"recovery": {
  "name": "恢复期",
  "daysRange": [14, 60],  // 原来是 [14, 55]
  ...
}
```

### 修改运动目标

调整保护期的运动目标：

```json
"protection": {
  ...
  "exerciseTarget": 20,  // 原来是 15
  ...
}
```

### 添加新的蛋白质食物

在对应阶段的数组中添加：

```json
"protection": [
  {
    "name": "清蒸鲈鱼",
    "amount": "50–80g",
    "meal": "午餐 / 晚餐",
    "note": "极少盐，不放油，易消化"
  },
  {
    "name": "新食物",       // 添加新食物
    "amount": "100g",
    "meal": "午餐",
    "note": "营养说明"
  }
]
```

### 修改血压正常范围

调整收缩压正常范围：

```json
"systolic": {
  "normal": [90, 140],     // 原来是 [90, 140]
  "dangerLow": 85,         // 调整危险低值
  "dangerHigh": 180,
  "warningHigh": 145       // 调整警告高值
}
```

### 修改检查时间

调整血脂检查的推荐时间：

```json
"firstLipid": {
  "name": "血脂检查（第一次）",
  "dueStartDays": 30,     // 原来是 28
  "dueEndDays": 60,       // 原来是 56
  ...
}
```

---

## ⚠️ 注意事项

1. **JSON 格式要求：**
   - 严格的JSON格式，不能有尾随逗号
   - 字符串必须用双引号 `"`
   - 数字不要用引号

2. **修改后刷新：**
   - 修改 `data.json` 后，刷新浏览器页面即可生效
   - 无需重新部署（GitHub Pages 会自动更新）

3. **验证JSON格式：**
   - 可以使用在线工具验证：https://jsonlint.com/
   - 确保没有语法错误

4. **备份：**
   - 修改前建议备份 `data.json`
   - 可以复制一份命名为 `data.json.backup`

---

## 📋 完整示例

完整的 `data.json` 文件结构请参考项目中的 `data.json` 文件。

---

## 🔄 未来扩展

可以在 `data.json` 中添加更多配置项，例如：
- 症状列表
- 用药提醒文字
- 警告消息模板
- 等等...

所有这些都可以通过修改JSON文件来调整，无需修改代码。
